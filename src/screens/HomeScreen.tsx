import { StyleSheet} from 'react-native'
import React from 'react'
import CustomIcon from '../components/CustomIcon'

const HomeScreen = () => {
  return (
    <CustomIcon name='home' size={100}></CustomIcon>
  )
}

export default HomeScreen

const styles = StyleSheet.create({})